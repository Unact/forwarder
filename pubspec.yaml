name: forwarder
description: Приложение для экспедиторов
version: 1.0.3+6

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  path_provider: any
  sqflite: any
  http: any
  intl: any
  sentry: any
  dotenv: any
  shared_preferences: any
  device_info: any
  package_info: any
  url_launcher: any
  signature_pad_flutter: any
  iboxpro_flutter:
    git:
      url: ssh://git@github.com/Unact/iboxpro_flutter.git

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - lib/data/schema.sql
    - .app_env
